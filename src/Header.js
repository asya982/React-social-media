import React from "react";

const Header = () => {
    return (
        <div>
            <h1>Welcome</h1>
            <button>home</button>
            <button>feed</button>
            <button>profile</button>
        </div>
    );
}

export default Header;